import { Empty } from '../../../project/shared';

// Rempli en Phase 4 (vue cross-projets des coffres).
export function ChestsAdmin() {
  return <Empty>Vue cross-projets des coffres — à venir.</Empty>;
}
